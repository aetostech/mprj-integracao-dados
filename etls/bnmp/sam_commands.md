# Relevant SAM Commands

## Local Build

`sam build --use-container`

## Local invoke

`sam local invoke {FunctionName}`

## Deployment

### First deployment

`sam deploy --guided`

### With `samconfig.toml` already created

`sam deploy`
